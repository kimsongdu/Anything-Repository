#include <stdio.h>

int main(void) {

	int total = 0;
	for (int i = 1; i <= 9; i++) {
		for (int j = 1; j <= 9; j++) {
			total = i * j;
			printf("%d x %d = %d " ,i, j, total);
		}
		printf("\n");
	}
	return 0;
}
